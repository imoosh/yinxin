package main

/*
#include <stdlib.h>
#include <string.h>
*/
import "C"

// 主函数（CGO需要）
func main() {
	// CGO库不需要主函数实现，但输出版本信息用于调试
	// fmt.Printf("iotvpn  Config Manager Plugin Library\n")
	// fmt.Printf("Version: %s\n", version.GetFullVersion())
	// fmt.Printf("Build Info:\n%s\n", version.GetBuildInfo())
}
